<%- include('header') %>

<h2>
    Edit Role:
</h2>

<a href="/admin/vagas">Return</a>

<div>
    <form method="POST">
        <label>Título:</label>
        <input type="text" name="titulo" value="<%= vaga.titulo %>">
        <br>
        <label>Descrição:</label>
        <textarea name="desc"><%= vaga.desc %></textarea>
        <br>
        <label>Categoria:</label>
        <select name="categoria">
            <% categorias.forEach( categoria => { %>
            <option value="<%= categoria.id %>" <% if(categoria.id == vaga.categoria){ %>selected<% } %>><%= categoria.categoria %></option>
            <% }) %>
        </select>
        <br>
        <button>Salvar</button>
</div>